public class Barcode implements Comparable<Barcode>{

    private String _zip;
    private int _checkDigit;

    public Barcode(String zip){
	if(_zip.length() != 5 ||
	   for(int index = 0; index < _zip.length(); index ++){
	       ((int)_zip.charAt(index) > 57 || (int)_zip.charA< 48){
		    false;
	       }
	   }
	   )
	   
